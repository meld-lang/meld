// 38 — Module Imports
// imp loads functions from other .meld files in the same directory.

imp mathlib

fnc main() -> () {
    println("square(7) = " + square(7))
    println("cube(3) = " + cube(3))
    println("imported module works!")
}
